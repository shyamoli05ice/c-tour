#include<stdio.h>
int main()
{
    int num,gpa;
    if(num<=80)
    printf(A+);
    else
    if(num<=70)
    printf(A);
    else
    if(num<=60)
    printf(A-);
    else
    if(num<=50)
    printf(B);
    else
    if(num<=40)
    printf(C);
    else
    if(num<=33)
    printf(D);
    else
    printf("F");

}
